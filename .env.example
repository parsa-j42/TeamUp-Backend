NODE_ENV=development
PORT=3000

# Postgres — use individual params (Neon: set DB_SSL=true)
DB_HOST=
DB_PORT=5432
DB_USERNAME=
DB_PASSWORD=
DB_DATABASE=
DB_SSL=false

# AWS Cognito
AWS_REGION=
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=

# Google Gemini
GEMINI_API_KEY=
# Model used for recommendations. Defaults to gemma-4-26b-a4b-it.
# Switchable to a Gemini model (e.g. gemini-2.5-flash) without code changes.
GEMINI_MODEL=gemma-4-26b-a4b-it

# CORS — space or comma separated origins (e.g. https://teamup.parsaj.dev)
# Defaults to http://localhost:5173 if not set
CORS_ORIGIN=http://localhost:5173

# Demo account email — used by the seed script to identify the shared demo user
DEMO_USER_EMAIL=
