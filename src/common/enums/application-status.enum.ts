export enum ApplicationStatus {
  PENDING = 'Pending',
  ACCEPTED = 'Accepted',
  DECLINED = 'Declined',
  INVITED = 'Invited',
  // WITHDRAWN = 'Withdrawn', // Optional: If users can withdraw applications
}
