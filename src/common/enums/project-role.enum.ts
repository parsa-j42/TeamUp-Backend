export enum ProjectRole {
  OWNER = 'Owner', // Changed from LEADER to OWNER based on ProjectPage mock data
  MEMBER = 'Member',
  MENTOR = 'Mentor', // Added based on CreateProjectPage mentor options
  // Add other roles if needed
}
